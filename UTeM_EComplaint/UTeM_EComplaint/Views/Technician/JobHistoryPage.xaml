<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UTeM_EComplaint.Views.JobHistoryPage"
             xmlns:vm="clr-namespace:UTeM_EComplaint.ViewModels" 
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit" 
             xmlns:model="clr-namespace:UTeM_EComplaint.Model" 
             xmlns:cell="clr-namespace:UTeM_EComplaint.Cells"
             BackgroundColor="{StaticResource BackgroundColor}"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <vm:JobHistoryViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <RefreshView IsRefreshing="{Binding IsBusy, Mode=OneWay}"
                         Command="{Binding RefreshCommand}">
                <CollectionView x:Name="listTask" BackgroundColor="Transparent" 
                                     SelectedItem="{Binding SelectedComplaint, Mode=TwoWay}"
                                     SelectionMode="Single"
                                     RemainingItemsThreshold="1"
                                     RemainingItemsThresholdReachedCommand="{Binding LoadMoreCommand}"
                                     SelectionChangedCommand="{Binding ItemSelectedCommand}"
                                     SelectionChangedCommandParameter="{Binding SelectedComplaint}"
                                     ItemsSource="{Binding ComplaintList}">
                    <CollectionView.Header>
                        <StackLayout>
                            <Label Text="Job History" FontSize="25" Margin="0,20" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" TextColor="Black" FontAttributes="Bold"/>
                        </StackLayout>
                    </CollectionView.Header>
                    <CollectionView.EmptyView>
                        <StackLayout>
                            <Label Text="There is no Completed Job" Style="{StaticResource DetailNoItem}" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
                        </StackLayout>                    </CollectionView.EmptyView>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="model:Complaint">
                            <cell:TechnicianCompletedCell/>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </RefreshView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>